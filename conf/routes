# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Http
GET     /requestHTTP     controllers.ApplicationHTTP.requestHTTP(type: String, apiKey: String, appId: String, latitude: Double, longitude: Double, swipeStart: Int, swipeEnd: Int, deviceId: String, equalityParam1: String, payload: String)

# Websocket
GET     /requestWS       controllers.ApplicationWS.requestWS(type: String, apiKey: String, appId: String, latitude: Double, longitude: Double, swipeStart: Int, swipeEnd: Int, deviceId: String, equalityParam1: String, payload: String)
