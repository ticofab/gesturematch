# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.aliveTest
GET     /request                    controllers.Application.requestWS(type: String, apiKey: String, latitude: Double, longitude: Double, swipeStart: Int, swipeEnd: Int, deviceId: String, equalityParam1: String, payload: String)
