# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Http
GET     /                 controllers.ApplicationHTTP.alive

# Websocket
GET     /api/open         controllers.ApplicationWS.openWS
