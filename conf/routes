# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.aliveTest
GET     /request                    controllers.Application.requestWS(requestType: String, latitude: Double, longitude: Double, swipeStart: Int, swipeEnd: Int, payload: String, equalityParam1: String)
