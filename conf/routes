# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Http
GET     /             controllers.ApplicationHTTP.alive

# Websocket
GET     /v1/open    controllers.ApplicationWS.openv1(os: String, deviceId: String)
