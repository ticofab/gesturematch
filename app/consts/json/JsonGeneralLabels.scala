package consts.json

object JsonGeneralLabels {
  // top keys
  val KIND = "kind"
  val TYPE = "type"
  val OUTCOME = "outcome"
  val REASON = "reason"
  val PAYLOAD = "payload"
  val GROUP_ID = "groupId"

  // top values
  val OK = "ok"
  val FAIL = "fail"
  val PARTIAL = "partial"
}
