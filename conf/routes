# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Http
GET     /             controllers.ApplicationHTTP.alive
GET     /db           controllers.ApplicationHTTP.dbTest

# Websocket
GET     /open         controllers.ApplicationWS.open
