# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /request/:requestType       controllers.Application.requestWS(requestType: String,
                                                                      latitude: Double,
                                                                      longitude: Double,
                                                                      swipeStart: Int,
                                                                      swipeEnd: Int,
                                                                      payload: String,
                                                                      equalityParam1: String)
